from pathlib import Path;txt=Path('backend/enhanced_gpt_ai.py').read_text(encoding='utf-8').splitlines();targets=['class GPTConfig','class AIResponse','class EnhancedIntentClassifier','class ResponseSynthesizer','def synthesize(','class EnhancedGPTStyleAI'];[print(t,'-,next((i for i,l in enumerate(txt,1) if t in l),None)) for t in targets] 
